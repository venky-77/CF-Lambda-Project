version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - pip install awscli --upgrade
  build:
    commands:
      - echo "Deploying CloudFormation stack"
      - aws cloudformation deploy --template-file template.yml --stack-name MyLambdaSchedulerStack
